language: ruby
rvm:
  - 2.2.9
install:
  - bundle update
  - bundle install
  - bundle exec jekyll build
  # - bundle exec htmlproofer ./_site --only-4xx --external_only --url-ignore "http://sda.tech/feed.xml"

script: true
sudo: false
